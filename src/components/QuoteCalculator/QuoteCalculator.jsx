import React from 'react';

const QuoteCalculator = ({ services, duration, addons }) => {
  // Calculate and display the quote based on selected options
  return (
    <div>
      <p>Total Quote: $XXX</p>
    </div>
  );
};

export default QuoteCalculator;
